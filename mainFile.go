package main


//import "fmt"
//import "github.com/Flipkart/artcli/utils"

//import   "github.com/ambsflip/goSamples/apicall"
// import "encoding/json"
// import "io/ioutil"
//import "os"
//import   "github.com/ambsflip/goSamples/PackageTest"
//import  "github.com/ambsflip/stringutil"

//var(
//  Version string
//  Build string
//)

// type ArtEntry struct {
//     artifact Artifact
//     status string
//     store string
//     external_download_url string
// }
// type Artifact struct {
//   package_name string
//   version string
//
// }

//func main() {
  //ListFileInPwd("jar")
  //fmt.Println(stringutil.Reverse("!selpmaxe oG ,olleH"))
  //cliSample()
  //fmt.Println("Exiting")
  //fmt.Println("Version, Build : ",Version,Build)

  //fmt.Println(average(98,93,77,81,84))

  //CONCURRENCY
  // for i := 0; i < 5; i++ {
  //     go f(i)
  //   }
  // var input string
  // fmt.Scanln(&input)
  // fmt.Println("input is :",input)

  // CHANNELS
  // var c chan string = make(chan string)
  // go pinger(c)
  // go ponger(c)
  // go printer(c)
  // var input string
  // fmt.Scanln(&input)

  //package - note the use of uppercase first alphabet for imported function
  //fmt.Println(MyMath.AverageOf(100,98,93,77,81,84))

  //witin the same package, fucntions in one file can refer to variables in other fileDescriptor
  // this can be used without qualifiers before the variable name.
  //PackageTest.GetConfigValues()
  //PackageTest.PrintValues()

  //fmt.Println(os.Args)
  //cmdLineSample()
  //cmdLineFlags()
  //postCall()
  //PostNewJenkins()
  //readFile()
  //regexsample();
  //readCredentials();
  //concatenateStrings();
  //URLFormatCheck()
  //StringReplace();
  //getWorkingDir()
  // fileExists,errFileExists := isFileExists("/Users/ambarish.a/Documents/myRepos/GoLangSamples/src/github.com/ambsflip/goSamples")
  // if errFileExists != nil {
  //     fmt.Println("errFileExists: ",errFileExists)
  // }
  // fmt.Println("fileExists: ",fileExists)
  // CreateLogFile()
  // convertTextToGo()
  // jsonProcessing()
  // yamlProcessing()
//}
